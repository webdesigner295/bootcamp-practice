import React from 'react'
import Child from './Child'

const Parent = ()=> {
    return(
        <div>
            {/* <Child name  = {props.name}/> */}
            <Child/>

        </div>
    )
}
export default Parent;